#include <stdio.h>
#include <stdlib.h>

int main(){
int a,i;

    printf("Digite um numero ");
    scanf("%d", &a);
    printf("\nDivisores de %d:", a);
    for (i=1;i<=a;i++)
        if (a%i==0)
            printf ("%d ",i);

    
}
