#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main()
{

    int num, ale;

    srand(time(0));
    
    
    printf ("Digite um numero: ");
    scanf ("%d", &num);
    
    ale = (rand()%(num - 0+1)+0);
    
    printf("Aleatorio gerado: %d", ale);
    
}
